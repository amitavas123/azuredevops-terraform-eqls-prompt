parameters:
  - name: step
    type: string
    values:
      - validate
      - plan
      - apply

steps:
  # Set up Terraform environment
  - task: UseTerraformVersion@0
    inputs:
      version: '$(terraformVersion)'
    displayName: 'Setup Terraform $(terraformVersion)'

  # Download SSH Key from secure files
  - task: DownloadSecureFile@1
    name: sshKey
    inputs:
      secureFile: 'id_rsa'
    displayName: 'Download SSH Private Key'

  # Setup SSH key permissions
  - script: |
      mkdir -p ~/.ssh
      cp $(sshKey.secureFilePath) ~/.ssh/id_rsa
      chmod 600 ~/.ssh/id_rsa
      cat $(sshKey.secureFilePath).pub > ~/.ssh/id_rsa.pub
    displayName: 'Configure SSH Key'
    condition: ne('${{ parameters.step }}', 'validate')

  # Initialize Terraform with remote backend
  - script: |
      cd $(terraformWorkingDirectory)
      terraform init \
        -backend-config="resource_group_name=$(backendResourceGroup)" \
        -backend-config="storage_account_name=$(backendStorageAccount)" \
        -backend-config="container_name=$(backendContainer)" \
        -backend-config="key=$(backendKey)" \
        -no-color
    displayName: 'Terraform Init'
    env:
      ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)
      ARM_CLIENT_ID: $(ARM_CLIENT_ID)
      ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
      ARM_TENANT_ID: $(ARM_TENANT_ID)

  # Terraform Format Check
  - script: |
      cd $(terraformWorkingDirectory)
      terraform fmt -check -recursive .
    displayName: 'Terraform Format Check'
    condition: eq('${{ parameters.step }}', 'validate')

  # Terraform Validate
  - script: |
      cd $(terraformWorkingDirectory)
      terraform validate
    displayName: 'Terraform Validate'
    condition: eq('${{ parameters.step }}', 'validate')
    env:
      ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)
      ARM_CLIENT_ID: $(ARM_CLIENT_ID)
      ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
      ARM_TENANT_ID: $(ARM_TENANT_ID)

  # Terraform Plan
  - script: |
      cd $(terraformWorkingDirectory)
      terraform plan \
        -var-file="env/dev/dev.tfvars" \
        -var="subscription_id=$(ARM_SUBSCRIPTION_ID)" \
        -var="public_key_path=~/.ssh/id_rsa.pub" \
        -no-color \
        -out=tfplan
    displayName: 'Terraform Plan'
    condition: eq('${{ parameters.step }}', 'plan')
    env:
      ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)
      ARM_CLIENT_ID: $(ARM_CLIENT_ID)
      ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
      ARM_TENANT_ID: $(ARM_TENANT_ID)

  # Save plan as artifact
  - task: PublishBuildArtifacts@1
    inputs:
      PathtoPublish: $(terraformWorkingDirectory)/tfplan
      ArtifactName: 'terraform-plan'
      publishLocation: 'Container'
    displayName: 'Publish Terraform Plan'
    condition: eq('${{ parameters.step }}', 'plan')

  # Terraform Apply
  - script: |
      cd $(terraformWorkingDirectory)
      terraform apply \
        -var-file="env/dev/dev.tfvars" \
        -var="subscription_id=$(ARM_SUBSCRIPTION_ID)" \
        -var="public_key_path=~/.ssh/id_rsa.pub" \
        -no-color \
        -auto-approve
    displayName: 'Terraform Apply'
    condition: eq('${{ parameters.step }}', 'apply')
    env:
      ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)
      ARM_CLIENT_ID: $(ARM_CLIENT_ID)
      ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
      ARM_TENANT_ID: $(ARM_TENANT_ID)

  # Save Terraform outputs as artifact
  - script: |
      cd $(terraformWorkingDirectory)
      terraform output -json > terraform-outputs.json
    displayName: 'Export Terraform Outputs'
    condition: eq('${{ parameters.step }}', 'apply')

  - task: PublishBuildArtifacts@1
    inputs:
      PathtoPublish: $(terraformWorkingDirectory)/terraform-outputs.json
      ArtifactName: 'terraform-outputs'
      publishLocation: 'Container'
    displayName: 'Publish Terraform Outputs'
    condition: eq('${{ parameters.step }}', 'apply')
